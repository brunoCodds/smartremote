<?xml version="1.0" encoding="utf-8"?>
<!--
    Grade de apps disponíveis para abrir na TV. O fundo/cantos do sheet
    em si já vêm de Widget.Fluent.BottomSheet (aplicado via
    bottomSheetDialogTheme no Theme.SmartRemote) — este XML só cuida
    do conteúdo interno.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="12dp"
    android:paddingTop="12dp"
    android:paddingBottom="24dp">

    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="@drawable/fluent_bg_sheet_handle" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:text="@string/apps_bottom_sheet_title"
        android:textAppearance="@style/TextAppearance.Fluent.SectionTitle" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerApps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:clipToPadding="false"
        android:overScrollMode="ifContentScrolls"
        tools:listitem="@layout/item_app" />

</LinearLayout>
